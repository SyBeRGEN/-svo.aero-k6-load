{
  "name": "svo.aero.k6",
  "version": "1.0.0",
  "description": "",
  "main": "scenarios/searchByDate.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "k6": "^0.0.0"
  }
}
